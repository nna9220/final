services:
  mysql-container:
    image: mysql:8.3.0
    environment:
      - MYSQL_ROOT_PASSWORD=Nanguyen1206@
    volumes:
      - /data:/var/lib/mysql
      - ./backupfile.sql:/docker-entrypoint-initdb.d/backup.sql
    command: --init-file /docker-entrypoint-initdb.d/backup.sql
  
  front-end:
    build:
      context: front-end
      dockerfile: Dockerfile
    ports:
      - 80:3000
    depends_on:
      - back-end

  back-end:
    build:
      context: back-end
      dockerfile: Dockerfile
    expose:
      - "5000"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql-container/registertopic
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=Nanguyen1206@
    depends_on:
      - mysql-container

  web-server:
    image: nginx:1.25.4-alpine
    ports:
      - 10072:80
      - 8443:443
    volumes:
      - ./nginx/conf/nginx.conf:/etc/nginx/conf.d/nginx.conf
      - ./nginx/conf/default.conf:/etc/nginx/conf.d/default.conf
      - ./front-end/build:/usr/share/nginx/html
    depends_on:
      - front-end

